name: Test-fail

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches:
      - "main"

jobs:
    fail-job:
        runs-on: ubuntu-slim
        steps:
            - name: Fail step
              run: exit 2