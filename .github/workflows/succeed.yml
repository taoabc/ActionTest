name: Test-succeed

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches:
      - "main"

jobs:
    succeed-job:
        runs-on: ubuntu-slim
        steps:
            - name: Succeed step
              run: echo "This step succeeds"